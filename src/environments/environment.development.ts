/**
 * Variáveis de ambiente do frontend (desenvolvimento).
 *
 * Usado automaticamente pelo `ng serve` via fileReplacements (angular.json).
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api',
};
